<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <parent>
        <groupId>com.daren.platform.urgent-project.build</groupId>
        <artifactId>compiled-bundle-settings</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <groupId>${groupId}</groupId>
    <artifactId>${project.artifactId}</artifactId>
    <version>${project.version}</version>

    <name>Urgent :: Modules - ${project.artifactId}</name>

    <packaging>pom</packaging>

    <properties>

    </properties>
    <modules>
        <module>${project.artifactId}-api</module>
        <module>${project.artifactId}-core</module>
        <module>${project.artifactId}-persist-jpa</module>
        <module>${project.artifactId}-entities</module>
        <module>${project.artifactId}-webapp-wicket</module>
    </modules>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.daren.${project.artifactId}</groupId>
                <artifactId>com.daren.${project.artifactId}.core</artifactId>
                <version>${project.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>com.daren.${project.artifactId}</groupId>
                <artifactId>com.daren.${project.artifactId}.api</artifactId>
                <version>${project.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>com.daren.${project.artifactId}</groupId>
                <artifactId>com.daren.${project.artifactId}.entities</artifactId>
                <version>${project.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>com.daren.${project.artifactId}</groupId>
                <artifactId>com.daren.${project.artifactId}.persist.openjpa</artifactId>
                <version>${project.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>com.daren.${project.artifactId}</groupId>
                <artifactId>com.daren.${project.artifactId}.webapp.wicket</artifactId>
                <version>${project.version}</version>
                <scope>provided</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>